# Redis Configuration (Requerido para producción)
# Obtén estos valores desde tu dashboard de Vercel > Storage > Redis Database

VITE_REDIS_URL=your_redis_url_here
# Ejemplo: redis://default:password@redis-host.upstash.io:port
